#include <stdio.h>
#include <string.h>

char [100][10]
int mian()
{
	int n;
	scanf("%d",&n);
	for(int i=0;i<n;i++){
		scanf("%s",&a[i]);
	}
	for(int i=0;i<n;i++){
		for(int j=i+1;j<n;j++){
			if(strcmp(a[i],a[j]);
			strcpy(temp,a[i]);
			strcpy(a[j],a[i]);
			strcpy(a[j],temp);
			}
	}
	for(int i=0;i<n;i++){
	printf("%s",a[i]);
	}
}
